#include "../include/pch.h"
/* fuction signature */
Time readFile(std::string file);

std::string getCurrentTime();

Result convertFromSecond(double seconds);

std::string formatDuration(Result res);

Duration converstion(double startServer, double inActivity);